# LUASCRIPT Language Support Matrix

Last updated: 2026-07-29

This matrix is evidence-based. A language is not production-ready until it has parser, lowering/transpilation, emitter, runtime or executable behavior, and integration coverage in the current repo.

Pre-production beta v0.1 readiness is tracked separately from full-language readiness. `npm run language:implemented:bidirectional` refreshes all implemented-lane reports, and `npm run beta:readiness` requires each implemented lane to have a fresh passing report above the 90% fixture threshold. `npm run beta:readiness:strict` additionally checks that the current strict-native blocker set remains closed. `npm run clarity:canon` includes that readiness audit, and `npm run beta:full` is the full local acceptance sweep. Java, Rust, Ruby, PHP, Dart, Go, Kotlin, Elm, and Gleam now have narrow native gates, but they are still limited slices rather than broad language support.

Stable Denali boundaries: [LUASCRIPT_PUBLIC_API_RUNTIME_CONTRACT.md](LUASCRIPT_PUBLIC_API_RUNTIME_CONTRACT.md) defines package entrypoints, root exports, CLI/API surface, Node/runtime expectations, package files, semver policy, compatibility policy, and release boundaries. Package identity is `luascript@1.0.1`, `src/unified_luascript.js` remains the current entrypoint, `npm run build` is a readiness smoke, native-runtime support requires a real runtime command plus a passing `language:<name>:bidirectional` gate, and target-runtime IR lanes do not substitute for native qualification. `examples/package/` contains the only installed-package examples; small JavaScript/`.ls` programs, actual-program fixtures, `examples/supported_math_showcase.ls`, and mathematical notation core/rehab V1 through V18 are repository-local evidence.

Denali release-facing language reports use provenance schema v2: live manifest,
fixture/source, implementation, runtime-probe, environment, and governing-doc
hashes are mandatory. `npm run test:compatibility-matrix` verifies that exact
binding for the 17 implemented-native lanes and records the current-host setup
matrix. A current-host pass is not cross-platform certification and does not
promote any row beyond its named slice.

Bidirectionality terms follow [LUASCRIPT_BIDIRECTIONALITY_CONTRACT.md](LUASCRIPT_BIDIRECTIONALITY_CONTRACT.md): native execution, source-to-IR, IR-to-target, target-runtime, emitted `.ls`, round-trip source identity, and semantic equivalence are separate claim layers. A row that says `bidirectional` is a named-slice gate claim unless the row also names explicit round-trip identity or semantic-equivalence evidence.

The first round-trip probe harness is `npm run test:roundtrip-probe`. It currently covers tiny JavaScript, `.ls`, Python, and Lua structural IR reparse probes plus JavaScript/Python runtime-output equivalence probes. It does not promote any row to full round-trip source identity or broad semantic equivalence.

The round-trip manifest/report now records a JS/.ls/Python/Lua bidirectionality layer map. JavaScript, `.ls`, Python, and Lua have named-slice evidence for native execution, source-to-IR, IR-to-target, target-runtime, and emitted `.ls`, and each now has seeded structural IR reparse evidence. Lua's evidence is one tiny normalized current-bridge Program-IR parity fixture, not source-text or token identity; only `.ls` has normalized source identity evidence, and its token identity is measured but non-gating. Semantic equivalence remains partial fixture stdout/diagnostic evidence only.

The release-shaped `.ls` source identity suite is `npm run test:source-identity-probe`. It currently covers 15 fixtures: 12 positive normalized `.ls` source identity, normalized parser-owned AST identity, and normalized current bridge IR identity checks plus 3 expected unsupported diagnostics. This is evidence for named `.ls` fixtures only; token-level text identity, runtime-output equivalence, broad lossless source recovery, and broad semantic equivalence remain separate unpromoted layers.

The Denali RC release-IR choice is [LUASCRIPT_RELEASE_IR_SURFACE_CONTRACT.md](LUASCRIPT_RELEASE_IR_SURFACE_CONTRACT.md): language gates continue through legacy object-tree Program IR `v0`, while current conformance evidence also derives canonical schema artifact `1.0.0` through a versioned one-way internal bridge. This does not promote any language row to schema-wide semantics, reverse conversion, source preservation, or public package IR API.

The first scoped 25-case edge-case matrix generator is `npm run test:edge-matrix`. It classifies manifest cases by value, control, scope, data, errors, target-specific behavior, and unsupported diagnostics, writes `artifacts/edge_matrix/edge-case-matrix-report.json` with category counts and case hashes, and stays scoped to current passing JavaScript/Python/Lua/`.ls` runtime evidence.

The unsupported diagnostics certification gate is `npm run test:unsupported-diagnostics`. It currently verifies 21 named current failures across JavaScript source, `.ls` source/meta repair, Python source, Lua source, the active JavaScript core fallback, and JavaScript/Lua target emitters. This proves intentional fail-closed boundaries for those named slices only; it is not a full unsupported-feature catalog.

The active-docs map is [INDEX.md](INDEX.md). Historical or archived docs are evidence only; they do not override this support matrix.

Language depth accession follows [LUASCRIPT_LANGUAGE_ACCESSION_RULES.md](LUASCRIPT_LANGUAGE_ACCESSION_RULES.md). Any row promotion or slice broadening must have manifest, parser coverage, lowering, emitter, native runtime, target runtime, docs, support matrix, claims check, and Denali ledger entry marked `MET` or explicitly `EXCLUDED` with rationale before this matrix can broaden the claim.

| Language path | Current tier | Evidence required before promotion | Current note |
| --- | --- | --- | --- |
| JavaScript input to Lua output | Practical V1 Ring 2 verified with targeted Ring 3 footholds | Keep `npm run language:javascript:bidirectional`, `npm test`, `npm run verify`, examples integration, `npm run test:actual-programs`, and `npm run test:ir-conformance` green | Best-supported path; Ring 2 now covers medium fixtures for closure state, nested object state, nested loops, break/continue, and short-circuit side effects. Targeted Ring 3 footholds cover plain template literals, zero-based index/length, and return-only `switch` statements with conditional expressions; `ring3_switch_conditional` runs through native JavaScript and emitted Lua/JavaScript, while emitted `.ls` shape is conformance-checked without runtime promotion and Python switch emission remains a target diagnostic. The conformance control-flow matrix additionally covers JavaScript if/else, while, numeric for, nested loops, break/continue, short-circuiting, early returns, switch statements, conditional expressions, emitted JavaScript/Lua/Python/`.ls` snippets, and JavaScript/Python runtime checks. The conformance function/scope matrix covers lexical closures, shadowing, mutation through closures, recursion, nested functions, return normalization, emitted JavaScript/Lua/Python/`.ls` snippets, and JavaScript/Python runtime checks. The data-structure matrix covers JavaScript nested array/object mutation, object literals, record fields, and emitted JavaScript/Python runtime checks for that slice. JavaScript `for-of`, `try/catch`, `throw`, tagged template literals, rest parameters, async functions, generator functions, object spread, and destructuring patterns remain explicit named unsupported diagnostics in these matrices and in `npm run test:unsupported-diagnostics`; extra-arity behavior is recorded as a target-native delta, not full JavaScript |
| TypeScript input | Partial V0.25 verified | Keep `npm run language:typescript:bidirectional`, `npm run beta:readiness`, and `npm run clarity:languages` green | New beta foundational lane: typed-JS small programs are parsed with the TypeScript compiler API, runtime-neutral type syntax is erased, canonical IR emits Lua, JavaScript, supported `.ls`, and Python, and unsupported classes/enums/generics/imports fail with explicit diagnostics; not full TypeScript |
| LUASCRIPT `.ls` JS-like syntax plus V0.16 meta layer to Lua/Python/JavaScript/`.ls` | Practical V0.16/V1 Ring 2 verified | Keep `npm run language:luascript:bidirectional`, `npm run test:luascript-meta`, `npm run test:source-identity-probe`, and dogfood/actual-program fixtures green | Supported `.ls` remains a JS-like LuaScript executable slice plus a top-level keyword-block meta layer for target policy, canonical repair blocks, and embedded fixture verification. Current supported `.ls` profile set is `portable_semantics_v1` as the implicit executable baseline, `portable_semantics_v1` as an explicit semantic-adapter profile, and `portable_v1` as an explicit cross-target policy profile. `meta_identity_contract_portable_slice.ls` now composes only those supported profiles, asserts `meta`/`repair`/`verify`/profile feature slices, proves Lua/JavaScript/Python/emitted-`.ls` stdout, and explicitly excludes classes, for-of, try/catch, and template literals. The `.ls` language manifest and unsupported-diagnostics catalog now explicitly certify named expected failures for `throw`, `for-of`, `try/catch`, and unsupported repair strategy without promoting those forms. The source-identity suite now adds 12 positive normalized `.ls` source/parser-owned-AST/IR identity checks across bindings, expressions, functions, conditionals, loops, arrays, objects, indexing, slicing, profile blocks, repair blocks, and verify blocks, plus 3 expected unsupported diagnostics; this is still named-fixture evidence only. V0.16 proves Lua `continue` resolution, `requires lua.goto`, `requires js.console`, `requires python.print`, `forbid lua.goto`, `forbid js.prototype`, `forbid python.imports`, JavaScript/Python native-continue policy metadata, Lua semantic adapters and canonical repairs for zero-based indexing, `.length`, `.slice`, JS truthiness, string coercion, packed multiple returns, Python target JS-truthiness execution through `__ls_truthy`/`__ls_and`/`__ls_or`, Python target zero-based indexing through `__ls_index`/`__ls_set_index`, Python target `.length` through `len(...)`, Python target `.slice(...)` through `__ls_slice`, Python target string coercion through `__ls_add`/`__ls_to_string`, Python target packed multiple returns through list emission, Lua/JavaScript/Python/`.ls` emitted-output contains/not-contains assertions, target-specific runtime stdout assertions, target-specific runtime-failure assertions, Lua/JavaScript/Python policy presence/absence assertions, explicit and implicit profile assertions, configured async diagnostics, unknown-policy diagnostics, cross-target `.ls` policy re-emission, and compile-time stripping. Repair blocks are supported for the current native parser's repair targets; emitted `.ls` repair identity is verified through current source-identity fixtures plus profile policy and `ls_repair` assertions, not broad lossless recovery. The data-structure matrix now adds a portable `.ls` length/slice/mutation fixture with emitted JavaScript/Python runtime checks; not a full distinct language spec yet |
| Mathematical notation core/rehab V18 | Practical narrow slice | Keep `examples/mathematical_notation_core.ls`, rehab V1/V2/V3/V4/V5/V6/V7/V8/V9/V10/V11/V12/V13/V14/V15/V16/V17/V18, `npm run test:actual-programs`, examples integration, and `npm run clarity:dogfood` green | Restores executable math function notation, Unicode arithmetic/comparison operators, `√`, superscript powers, constants, subscript identifiers, Unicode arrow lambdas, pipelines, ranges, `let ... in`, implicit multiplication, exponentiation, `mod`, pattern branches, composition, operator sections, tuple callbacks, tuple-object returns, set operators, default parameters, bound symbolic identifiers, bare unbound `i` as the imaginary unit, complex arithmetic, runtime `fft`/`inverse_fft` helpers, deterministic series/calculus helper checks, bracketed native symbolic binder forms, braced math-native lower/upper binder notation, native derivative/limit binder notation, definite integral differential shorthand, bare definite-integral bodies, executable physics/EE vector-calculus operators/helpers, dimension-aware unit helpers, electrical phasor/impedance helpers, operator-level unit arithmetic, matrix/linear physics helpers, symbolic variables/equations, formula rendering, simplification/evaluation, single-variable linear symbolic solving, dimension-aware symbolic variables, symbolic dimension assertions, small linear symbolic equation-system solving, symbolic derivatives, symbolic substitution, dimension-aware RC/RL transfer functions, symbolic impedances, symbolic voltage-divider helpers, executable frequency-response evaluation, RC/RL cutoff and time constants, RLC resonance/Q/bandwidth helpers, symbolic series-RLC impedance, swept frequency-response arrays, Bode column extraction, peak/trough/nearest lookup, monotonic checks, and dB crossing estimation; full symbolic physics/EE algebra remains future work |
| Full Unicode mathematical DSL | Experimental | Dedicated grammar, parser, emitter/runtime semantics, and end-to-end tests | `examples/experimental/mathematical_showcase.ls` remains design material, not broad baseline support; it now parses/emits and executes end to end as an experimental dogfood fixture |
| Lua input | Partial V2.1 verified | Expand the V2.1 manifest until broader Lua syntax has parse, lowering, emitter, runtime, and integration coverage | `npm run test:lua-input` / `npm run language:lua:bidirectional` verifies Lua source -> canonical IR -> Lua, JavaScript, supported `.ls`, and Python for the documented V2.1 slice, now including `#` length, multiple assignment normalization, `ipairs` loops, table index read/write, Lua `math.max`/`math.floor` output, and emitted-Python runtime proof. Lua one-based computed table indexes now lower to Python zero-based indexes for the named slice. The data-structure matrix adds a Lua table record-field fixture with emitted JavaScript runtime proof; metatables, `require`, varargs, non-`ipairs` generic `for`, complex table keys, method-call syntax, and mixed array/object table constructors remain explicit unsupported boundaries. `npm run test:unsupported-diagnostics` now certifies the named varargs, metatables, `require`, and complex-table-key diagnostics |
| Python | Partial V1.3 verified | Expand `npm run language:python:bidirectional` from the current V1.3 slice before promoting broader Python | Python V1.3 covers V1.2 plus bounded sequence slices through `sequence_slice_access.py`: native Python and emitted Lua/JavaScript/`.ls`/Python agree for string and list slices. V1.3 still covers list `append`/`pop`, list and dict membership, nested list/dict mutation, string/list/dict indexing helpers, simple chained comparisons, loop `break`, range loops including positive and negative-step variants, JS/Lua/LUASCRIPT-to-Python reverse fixtures, and broader unsupported diagnostics. Slice steps and slice assignment remain explicit unsupported diagnostics. The conformance control-flow matrix adds Python range lowering to emitted JavaScript/Lua/Python/`.ls` snippets plus Python/JavaScript runtime checks, while Python source `continue` remains an explicit unsupported diagnostic in this slice. `npm run test:unsupported-diagnostics` now certifies named Python diagnostics for slice steps, slice assignment, and source `continue`. The function/scope matrix adds emitted-Python runtime checks for closure mutation, shadowing, recursion, nested functions, and return normalization; extra JavaScript call arguments remain a target-native delta because emitted Python preserves strict Python arity. The data-structure matrix adds native/emitted JavaScript/Python runtime checks for Python list/dict mutation, membership, nested reads/writes, list iteration, and length |
| Ruby | Experimental with V0.6 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:ruby:ir-targets` verifies a method-oriented slice with arrays, simple hashes, hash mutation, inclusive numeric ranges, boolean flow, `break`/`next`, string indexing/length, indexed reads/writes, length/size, math calls, and output through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:ruby:bidirectional` now executes the narrow native Ruby fixture slice |
| PHP | Experimental with V0.6 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:php:ir-targets` verifies a function-oriented slice with numeric arrays, simple associative arrays with string identifier keys, indexed reads/writes, boolean flow, `break`/`continue`, `strlen`, `for`, variable-backed `foreach`, `count`, math calls, and output through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:php:bidirectional` now executes the narrow native PHP fixture slice |
| Dart | Experimental with V0.6 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:dart:ir-targets` verifies a top-level function/list/loop slice with typed lists, indexed reads/writes, `for`, simple `for ... in`, boolean flow, `break`/`continue`, string length/indexing, math calls, and output through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:dart:bidirectional` now executes the narrow native Dart fixture slice |
| C# | Partial | C# V0.5 Program/static-method small-program slice only | `npm run language:csharp:bidirectional` verifies native `dotnet` execution plus emitted Lua, JavaScript, LUASCRIPT .ls, and C# outputs; V0.5 covers foreach over arrays including `continue`, `.Length`, string indexing, string interpolation, and common `Math.*` calls, while broader C# remains unsupported |
| C | Partial | C V0.3 record small-program slice only | `npm run language:c:bidirectional` verifies native clang execution plus emitted Lua, JavaScript, LUASCRIPT .ls, C, and C++ outputs; V0.3 covers free functions, record-like `struct` definitions with public scalar fields, struct initialization, struct member access/mutation, struct parameters/returns, array parameters with explicit count, `main`, locals, assignments, conditionals, `while`, numeric `for`, numeric arrays/indexing, static-array length through `sizeof(array) / sizeof(array[0])`, string length through `strlen`, string indexing, character literals, `break`/`continue`, `printf`/`puts`, and common math calls, while general pointers, struct array fields, macros, manual memory management, and broader C remain unsupported |
| C++ | Partial | C++ V0.4 class-method small-program slice only | `npm run language:cpp:bidirectional` verifies native clang++ execution plus emitted Lua, JavaScript, LUASCRIPT .ls, C, and C++ outputs; V0.4 covers free functions, simple public-field class/struct records, aggregate initialization, record member access/mutation, record parameters/returns, simple public constructors using `this->field` assignment, simple public instance methods lowered to canonical IR functions, raw array parameters with explicit count, `main`, locals, assignments, conditionals, `while`, numeric `for`, numeric arrays/indexing, `std::string` size/index basics, character literals, `break`/`continue`, `std::cout`, and common `std::math` calls, while inheritance, templates, exceptions, vectors, lambdas, dynamic allocation, non-public members, declarations without bodies, and broader C++ remain unsupported |
| Java | Experimental with V0.6 target-runtime lane plus narrow V1 native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:java:ir-targets` verifies a single-class static-method slice with primitive arrays, indexed reads/writes, enhanced `for`, boolean flow, `break`/`continue`, `.length`, `String.length()`, `String.charAt`, `Math.*`, and `System.out` through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:java:bidirectional` now compiles with `javac`, executes with `java`, and verifies the narrow native Java fixture slice |
| Go | Experimental with V0.25 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:go:ir-targets` verifies a package-function slice with typed parameters/returns, `var` and `:=` locals, primitive arrays, `range` over arrays, conditionals, while-style `for`, numeric `for`, `break`/`continue`, simple structs, field access/mutation, `fmt.Println`, `len`, and `math.Sqrt` through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:go:bidirectional` now executes the narrow native Go fixture slice |
| Rust | Experimental with V0.25 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:rust:ir-targets` verifies a free-function slice with typed parameters/returns, `let`/`mut` locals, primitive arrays, array iteration, numeric ranges, conditionals, `while`, `break`/`continue`, simple structs, field access/mutation, `println!`, `.len()`, and float `.sqrt()` through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:rust:bidirectional` now compiles with `rustc`, executes native Rust fixtures, and verifies the narrow native Rust fixture slice |
| Kotlin | Experimental with V0.25 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:kotlin:ir-targets` verifies a free-function slice with typed parameters/returns, `val`/`var` locals, primitive arrays, array iteration, numeric ranges, conditionals, `while`, `break`/`continue`, data-class record fields, field access/mutation, `println`, `.size`/`.length`, and `Math.*` through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:kotlin:bidirectional` now compiles with `kotlinc`, runs on Java, and verifies the narrow native Kotlin fixture slice |
| Elm | Experimental with V0.6 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:elm:ir-targets` verifies an expression-oriented top-level definition slice with boolean/comparison expressions including `||` and `&&`, unary `not`, nested calls, single-line `let ... in`, literal list access, literal/top-level-constant `length`, string concatenation with `++`, and output through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:elm:bidirectional` now compiles with `elm make`, runs the generated worker under Node, and verifies the narrow native Elm fixture slice |
| Gleam | Experimental with V0.6 target-runtime lane plus narrow native gate | Broader parser/lowering coverage before promotion beyond the named slice | `npm run language:gleam:ir-targets` verifies a function/let/if slice with boolean/comparison expressions including `||` and `&&`, nested calls, list helper calls, local-constant `list_length`/`string_length`, and output through canonical IR to emitted Lua, JavaScript, supported `.ls`, Python, and C# outputs; `npm run language:gleam:bidirectional` now runs the same slice with `gleam run` and verifies the narrow native Gleam fixture slice |

Promotion rule: do not raise a language tier from archived reports, generated snapshots, tokenizer-only tests, parser-only tests, or completion summaries. Promotion requires current tests that compile and verify behavior. The detailed slice rules live in [LANGUAGE_COMPLETION_RULES.md](LANGUAGE_COMPLETION_RULES.md).
