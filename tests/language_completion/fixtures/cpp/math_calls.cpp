#include <cmath>
#include <iostream>

int main()
{
    double root = std::sqrt(81);
    double neg = -std::abs(-4);
    double power = std::pow(2, 2);
    std::cout << "cpp_math " << root << " " << neg << " " << power << std::endl;
}
