#include <math.h>
#include <stdio.h>

int main(void)
{
    double root = sqrt(81);
    double neg = -fabs(-4);
    double power = pow(2, 2);
    printf("c_math %.0f %.0f %.0f\n", root, neg, power);
}
